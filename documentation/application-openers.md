# Application openers — one per chain

Paste the block for the chain you are applying to as the **first paragraph**. It
answers the "why this chain" criterion that Solana ("Only Possible on Solana") and
Starknet ("embeddedness with the ecosystem") score directly.

**Rule: never open with the 27-chain story.** It answers "why any chain" and reads
as uncommitted. Portability goes last, as evidence of capability.

Generated from `src/config/profiles.js` — edit there, not here.

---

## Polygon  `polygon.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Polygon** at `polygon.xono.ai`, where a tile is **an ERC-721 NFT**.
> AI Guardian agents watch your territory around the clock and defend a claim even while you are offline.

**Why Polygon specifically** — Polygon settles to Ethereum while keeping a claim cheap enough that buying and upgrading tiles stays a game move rather than a financial decision.

**Verifiable without contacting us:** `https://polygon.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Polygon, plus the live Polygon chain head read
from Polygon's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | MATIC |
| Wallets | — |
| Adapter family | `evm` |

---

## Avalanche  `avalanche.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Avalanche** at `avalanche.xono.ai`, where a tile is **an ERC-721 NFT**.
> Every claim, upgrade and raid is a real transaction you can look up yourself on the block explorer.

**Why Avalanche specifically** — Avalanche finalises a transaction in about a second, so your tile is provably yours before the map finishes animating the claim.

**Verifiable without contacting us:** `https://avalanche.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Avalanche, plus the live Avalanche chain head read
from Avalanche's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | AVAX |
| Wallets | Core, MetaMask, Coinbase Wallet |
| Adapter family | `evm` |

---

## Base  `base.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Base** at `base.xono.ai`, where a tile is **an ERC-721 NFT**.
> The whole game is onchain on Base — tiles, trades and upgrades, all from the wallet you already use.

**Why Base specifically** — Base is an Ethereum L2 with Coinbase account onboarding, so you can go from signing up to owning a tile without bridging or managing a seed phrase.

**Verifiable without contacting us:** `https://base.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Base, plus the live Base chain head read
from Base's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | ETH |
| Wallets | Coinbase Wallet, MetaMask, Rabby |
| Adapter family | `evm` |

---

## Arbitrum One  `arbitrum.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Arbitrum One** at `arbitrum.xono.ai`, where a tile is **an ERC-721 NFT**.
> A full game loop — claim, upgrade, raid, trade — running at rollup speed on Arbitrum.

**Why Arbitrum One specifically** — Arbitrum gives you Ethereum-secured ownership at rollup fees, so claiming, upgrading and raiding confirm fast enough to feel like a game loop.

**Verifiable without contacting us:** `https://arbitrum.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Arbitrum One, plus the live Arbitrum One chain head read
from Arbitrum One's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | ETH |
| Wallets | — |
| Adapter family | `evm` |

---

## Ronin  `ronin.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Ronin** at `ronin.xono.ai`, where a tile is **an ERC-721 NFT**.
> You are playing on a chain used only for games, next to the other titles already in your Ronin wallet.

**Why Ronin specifically** — Ronin is built for games and nothing else, so your tiles share blockspace with other games instead of competing with DeFi for gas.

**Verifiable without contacting us:** `https://ronin.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Ronin, plus the live Ronin chain head read
from Ronin's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | RON |
| Wallets | Ronin Wallet, MetaMask, Browser Wallet |
| Adapter family | `evm` |

---

## BNB Smart Chain  `bnb.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> BNB Smart Chain** at `bnb.xono.ai`, where a tile is **a BEP-721 NFT**.
> A finished game you can play today — claim your first tile in a couple of taps with the BNB you hold.

**Why BNB Smart Chain specifically** — BNB Chain pairs three-second blocks with wallet distribution straight out of Binance, so if you already hold BNB you can claim a tile without opening a new account.

**Verifiable without contacting us:** `https://bnb.xono.ai/ecosystem` reports this
deployment's own traction, scoped to BNB Smart Chain, plus the live BNB Smart Chain chain head read
from BNB Smart Chain's own node.

| | |
|---|---|
| Native asset | a BEP-721 NFT |
| Currency | BNB |
| Wallets | — |
| Adapter family | `evm` |

---

## OP Mainnet  `optimism.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> OP Mainnet** at `optimism.xono.ai`, where a tile is **an ERC-721 NFT**.
> An open map where every claim is a public record anyone across the Superchain can check.

**Why OP Mainnet specifically** — OP Mainnet is part of the Superchain, where one deployment standard is shared by every OP Stack chain, so territory claimed here is not stranded on a single rollup.

**Verifiable without contacting us:** `https://optimism.xono.ai/ecosystem` reports this
deployment's own traction, scoped to OP Mainnet, plus the live OP Mainnet chain head read
from OP Mainnet's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | ETH |
| Wallets | — |
| Adapter family | `evm` |

---

## Scroll  `scroll.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Scroll** at `scroll.xono.ai`, where a tile is **an ERC-721 NFT**.
> Ownership of your tile is backed by a zero-knowledge validity proof, not by taking our word for it.

**Why Scroll specifically** — Scroll is a zkEVM, so every claim on the map is backed by a validity proof settled to Ethereum instead of being taken on trust.

**Verifiable without contacting us:** `https://scroll.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Scroll, plus the live Scroll chain head read
from Scroll's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | ETH |
| Wallets | — |
| Adapter family | `evm` |

---

## Celo  `celo.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Celo** at `celo.xono.ai`, where a tile is **an ERC-721 NFT**.
> Built for phones first — claim the block you actually live on from your pocket, no desktop needed.

**Why Celo specifically** — Celo is mobile-first and lets fees be paid in stablecoins, so you can claim the block you actually live on from your phone without first buying a gas token.

**Verifiable without contacting us:** `https://celo.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Celo, plus the live Celo chain head read
from Celo's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | CELO |
| Wallets | Valora, MiniPay, MetaMask |
| Adapter family | `evm` |

---

## Moonbeam  `moonbeam.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Moonbeam** at `moonbeam.xono.ai`, where a tile is **an ERC-721 NFT on Polkadot**.
> One map reachable from across Polkadot, so your neighbours are not limited to a single chain.

**Why Moonbeam specifically** — Moonbeam runs the same EVM build inside Polkadot and can message other parachains over XCM, so your territory sits in an ecosystem a standalone EVM deployment never reaches.

**Verifiable without contacting us:** `https://moonbeam.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Moonbeam, plus the live Moonbeam chain head read
from Moonbeam's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT on Polkadot |
| Currency | GLMR |
| Wallets | — |
| Adapter family | `evm` |

---

## Beam  `beam.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Beam** at `beam.xono.ai`, where a tile is **an ERC-721 NFT**.
> Blockspace built for games, so claims and raids stay quick even when the map is busy.

**Why Beam specifically** — Beam is an Avalanche L1 dedicated to gaming, so your tiles live on blockspace built for game traffic rather than shared with financial activity.

**Verifiable without contacting us:** `https://beam.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Beam, plus the live Beam chain head read
from Beam's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | BEAM |
| Wallets | — |
| Adapter family | `evm` |

---

## Oasys  `oasys.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Oasys** at `oasys.xono.ai`, where a tile is **an ERC-721 NFT**.
> A game-native home in Japan and Asia — claim territory alongside the players already on Oasys.

**Why Oasys specifically** — Oasys was designed with Japanese game studios for game workloads, so you are claiming territory in an ecosystem whose players are already game-native.

**Verifiable without contacting us:** `https://oasys.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Oasys, plus the live Oasys chain head read
from Oasys's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | OAS |
| Wallets | — |
| Adapter family | `evm` |

---

## SKALE Nebula Gaming Hub  `skale.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> SKALE Nebula Gaming Hub** at `skale.xono.ai`, where a tile is **an ERC-721 NFT**.
> An indie-built game with zero gas: play a whole session of claiming and raiding without paying a fee.

**Why SKALE Nebula Gaming Hub specifically** — SKALE charges no gas at all, so you can claim, upgrade and trade tiles all day without ever paying a fee or touching an on-ramp.

**Verifiable without contacting us:** `https://skale.xono.ai/ecosystem` reports this
deployment's own traction, scoped to SKALE Nebula Gaming Hub, plus the live SKALE Nebula Gaming Hub chain head read
from SKALE Nebula Gaming Hub's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | sFUEL (gasless — no fee to claim) |
| Wallets | — |
| Adapter family | `evm` |

---

## Hedera  `hedera.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Hedera** at `hedera.xono.ai`, where a tile is **an ERC-721 NFT on Hedera**.
> A claim costs a predictable fraction of a cent, so you always know the price before you tap.

**Why Hedera specifically** — Hedera runs enterprise-grade aBFT consensus and prices transactions in fixed USD terms, so a claim costs the same during a market spike as it did yesterday.

**Verifiable without contacting us:** `https://hedera.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Hedera, plus the live Hedera chain head read
from Hedera's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT on Hedera |
| Currency | HBAR |
| Wallets | — |
| Adapter family | `evm` |

---

## Injective  `injective.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Injective** at `injective.xono.ai`, where a tile is **an ERC-721 NFT**.
> Your Guardian is an autonomous agent that prices and defends your territory while you are away.

**Why Injective specifically** — Injective is built for on-chain finance and autonomous agents — exactly what a Guardian is: a program that holds, prices and defends your territory.

**Verifiable without contacting us:** `https://injective.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Injective, plus the live Injective chain head read
from Injective's own node.

| | |
|---|---|
| Native asset | an ERC-721 NFT |
| Currency | INJ |
| Wallets | — |
| Adapter family | `evm` |

---

## Solana  `solana.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Solana** at `solana.xono.ai`, where a tile is **an SPL NFT**.
> Built for players on mobile — claim land from your phone in seconds, as fast as a game input.

**Why Solana specifically** — Solana confirms in well under a second, so claiming a tile registers like a game input instead of a transaction you wait on.

**Verifiable without contacting us:** `https://solana.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Solana, plus the live Solana chain head read
from Solana's own node.

| | |
|---|---|
| Native asset | an SPL NFT |
| Currency | SOL |
| Wallets | — |
| Adapter family | `solana` |

---

## TON  `ton.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> TON** at `ton.xono.ai`, where a tile is **a TON NFT item**.
> Plays entirely inside Telegram — nothing to install, and an invite is just a forwarded message.

**Why TON specifically** — TON runs inside Telegram, so you claim territory without leaving the chat you already have open — and an invite is just a forwarded message.

**Verifiable without contacting us:** `https://ton.xono.ai/ecosystem` reports this
deployment's own traction, scoped to TON, plus the live TON chain head read
from TON's own node.

| | |
|---|---|
| Native asset | a TON NFT item |
| Currency | TON |
| Wallets | — |
| Adapter family | `ton` |

---

## Aptos  `aptos.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Aptos** at `aptos.xono.ai`, where a tile is **a Move object**.
> Each tile is a native Move object you hold directly, and simultaneous claims never queue up.

**Why Aptos specifically** — On Aptos your tile is a first-class Move object with its own ownership rules, and transactions execute in parallel so simultaneous claims do not queue behind each other.

**Verifiable without contacting us:** `https://aptos.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Aptos, plus the live Aptos chain head read
from Aptos's own node.

| | |
|---|---|
| Native asset | a Move object |
| Currency | APT |
| Wallets | — |
| Adapter family | `aptos` |

---

## Sui  `sui.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Sui** at `sui.xono.ai`, where a tile is **a Sui object**.
> Your tile is a native Move object you own outright, so claiming stays instant even on a crowded map.

**Why Sui specifically** — On Sui your tile is an owned object, so claiming it takes the single-owner fast path instead of full consensus ordering and many players can carve up the map at once.

**Verifiable without contacting us:** `https://sui.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Sui, plus the live Sui chain head read
from Sui's own node.

| | |
|---|---|
| Native asset | a Sui object |
| Currency | SUI |
| Wallets | — |
| Adapter family | `sui` |

---

## Starknet  `starknet.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Starknet** at `starknet.xono.ai`, where a tile is **a Cairo ERC-721 token**.
> Who owns which tile is settled by a proof on Ethereum, not by trusting our server.

**Why Starknet specifically** — Starknet proves execution with STARKs and settles those proofs on Ethereum, so who owns which tile is verified by a proof rather than trusted to our server.

**Verifiable without contacting us:** `https://starknet.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Starknet, plus the live Starknet chain head read
from Starknet's own node.

| | |
|---|---|
| Native asset | a Cairo ERC-721 token |
| Currency | STRK |
| Wallets | — |
| Adapter family | `starknet` |

---

## Cardano  `cardano.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Cardano** at `cardano.xono.ai`, where a tile is **a native Cardano asset**.
> Your tile is a native Cardano asset sitting in your own wallet — no NFT contract in between.

**Why Cardano specifically** — On Cardano your tile is a native asset tracked by the ledger itself, so no NFT contract stands between you and the deed.

**Verifiable without contacting us:** `https://cardano.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Cardano, plus the live Cardano chain head read
from Cardano's own node.

| | |
|---|---|
| Native asset | a native Cardano asset |
| Currency | ADA |
| Wallets | — |
| Adapter family | `cardano` |

---

## NEAR  `near.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> NEAR** at `near.xono.ai`, where a tile is **a NEP-171 token**.
> AI Guardian agents defend your land natively on NEAR, and your deed reads as a name, not a hex string.

**Why NEAR specifically** — NEAR gives you a human-readable named account, so the deed to your territory reads alice.near instead of a hex string nobody can check at a glance.

**Verifiable without contacting us:** `https://near.xono.ai/ecosystem` reports this
deployment's own traction, scoped to NEAR, plus the live NEAR chain head read
from NEAR's own node.

| | |
|---|---|
| Native asset | a NEP-171 token |
| Currency | NEAR |
| Wallets | — |
| Adapter family | `near` |

---

## Stellar  `stellar.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Stellar** at `stellar.xono.ai`, where a tile is **a Soroban asset**.
> Soroban contracts plus Stellar fees so small that claiming a tile costs far less than a cent.

**Why Stellar specifically** — Stellar has moved value between real-world currencies cheaply for a decade and Soroban adds the contracts, so a game about real territory settles on real payment rails.

**Verifiable without contacting us:** `https://stellar.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Stellar, plus the live Stellar chain head read
from Stellar's own node.

| | |
|---|---|
| Native asset | a Soroban asset |
| Currency | XLM |
| Wallets | — |
| Adapter family | `stellar` |

---

## Algorand  `algorand.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Algorand** at `algorand.xono.ai`, where a tile is **an Algorand Standard Asset (ASA)**.
> Each tile is a native Algorand asset held in your wallet, final one block after you claim it.

**Why Algorand specifically** — An Algorand Standard Asset makes your tile a native ledger asset with single-block finality, so the claim is irreversible the moment it lands and no smart contract sits between you and the deed.

**Verifiable without contacting us:** `https://algorand.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Algorand, plus the live Algorand chain head read
from Algorand's own node.

| | |
|---|---|
| Native asset | an Algorand Standard Asset (ASA) |
| Currency | ALGO |
| Wallets | — |
| Adapter family | `algorand` |

---

## MultiversX  `multiversx.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> MultiversX** at `multiversx.xono.ai`, where a tile is **a native ESDT**.
> Tiles are native ESDT assets held by the protocol itself, so no NFT contract stands in between.

**Why MultiversX specifically** — MultiversX issues NFTs as native ESDT assets, so your tile is held by the protocol itself and capacity grows with the shards as more of the map is claimed.

**Verifiable without contacting us:** `https://multiversx.xono.ai/ecosystem` reports this
deployment's own traction, scoped to MultiversX, plus the live MultiversX chain head read
from MultiversX's own node.

| | |
|---|---|
| Native asset | a native ESDT |
| Currency | EGLD |
| Wallets | — |
| Adapter family | `multiversx` |

---

## Radix  `radix.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Radix** at `radix.xono.ai`, where a tile is **a native non-fungible resource**.
> Tiles are native resources, and your wallet spells out in plain words exactly what you are signing.

**Why Radix specifically** — On Radix your tile is a native resource enforced by the ledger rather than by contract bookkeeping, and the transaction manifest shows you exactly what you are signing away.

**Verifiable without contacting us:** `https://radix.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Radix, plus the live Radix chain head read
from Radix's own node.

| | |
|---|---|
| Native asset | a native non-fungible resource |
| Currency | XRD |
| Wallets | — |
| Adapter family | `radix` |

---

## Tezos  `tezos.xono.ai`

**Opener**

> CryptoLand is a geospatial territory game over the real world — a 16,384 × 16,384
> tile grid, 268,435,456 claimable tiles of roughly 2.4 km². It is **live on
> Tezos** at `tezos.xono.ai`, where a tile is **an FA2 token**.
> Tiles are FA2 tokens and the rules of the map are voted on-chain by players, not set by a studio.

**Why Tezos specifically** — Tezos amends itself by on-chain vote and standardises tokens as FA2, which matches a game whose territory rules are meant to be settled by its players.

**Verifiable without contacting us:** `https://tezos.xono.ai/ecosystem` reports this
deployment's own traction, scoped to Tezos, plus the live Tezos chain head read
from Tezos's own node.

| | |
|---|---|
| Native asset | an FA2 token |
| Currency | XTZ |
| Wallets | — |
| Adapter family | `tezos` |

---

## Closing paragraph (use on every application, last)

> The same game runs natively on 27 chains from one codebase — 13 adapter families
> behind a single 24-function interface, contract-tested. That is offered here as
> evidence that the team ships and maintains real cross-ecosystem infrastructure,
> not as a suggestion that this chain is interchangeable. Each deployment has its own
> bundle, database and backend; nothing is shared.

## Honesty block (required — matches what the product already says)

> No NFT contract is deployed yet, so on-chain minting is stubbed and every
> deployment reports zero on-chain mints. The worlds are seeded demo data with
> chain-correct addresses and modelled retention, not real players. Both facts are
> published on the product itself. This grant funds exactly that gap.
