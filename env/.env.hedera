# CryptoLand build config — Hedera — HBAR Foundation grants
# Usage: npm run build:chain hedera   (emits dist-hedera/)
VITE_API_BASE=
VITE_CHAIN=hedera
VITE_CONTRACT_HEDERA=
VITE_MARKETPLACE_HEDERA=
VITE_TOKEN_HEDERA=
# Optional: override the default public RPC with a paid endpoint (Alchemy etc.)
VITE_RPC_HEDERA=
