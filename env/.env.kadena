# CryptoLand build config — Kadena — Eco Grants ($100M fund)
# Usage: npm run build:chain kadena   (emits dist-kadena/)
VITE_API_BASE=
VITE_CHAIN=kadena
VITE_CONTRACT_KADENA=
VITE_MARKETPLACE_KADENA=
VITE_TOKEN_KADENA=
# Optional: override the default public RPC with a paid endpoint (Alchemy etc.)
VITE_RPC_KADENA=
