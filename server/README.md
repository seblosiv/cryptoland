## Setup

1. Copy `.env.example` to `.env`
2. Fill in your NOWPayments API keys
3. Run: `pip install -r requirements.txt`
4. Run: `uvicorn main:app --reload`

⚠️ Never commit `.env` to version control.
